export interface ProfileData {
  name: string;
  title: string;
  imageUrl: string;
  linksUrl: string;
}

export interface JourneyData {
  title: string;
  description: string;
  highlightedText: string;
}
